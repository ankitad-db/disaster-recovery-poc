# Core framework dependencies.
# The replication engine is FIRST-PARTY (src/databricks_dr/common/native), built on
# the public MLflow client + databricks-sdk -- there is no third-party engine to
# install or keep in sync with MLflow releases.
mlflow>=2.16
databricks-sdk>=0.30
PyYAML>=6.0
click>=8.1

# Seeding the POC model (01_seed_primary) trains a small sklearn model.
# Present by default on Databricks ML runtimes; listed for off-cluster/local use.
scikit-learn>=1.3
