# AWS assets request — Workspace Secrets DR (fully specified)

Provision the S3 + KMS + IAM + CRR assets for the workspace-secrets DR flow. All
names, regions, and policies are specified below — please create exactly these.
Where a value is generated by a resource created in this same ticket (KMS key id,
UC external id), the placeholder says who supplies it; nothing requires a design
decision.

## Fixed facts
- **AWS account:** `049629455384`
- **Regions:** `us-east-2` (primary), `us-west-2` (secondary)
- **Databricks workspaces:** `fevm-fevm-ps-dr-us-east-2` (primary) / `fevm-krish-dr-fevm-us-west-2` (secondary)
- **DR service principal:** `ad-dr-spn` (`dc5781bc-c86f-4808-a00c-930062f91f17`)

---

## 1. S3 buckets (create both)
| Name | Region | Versioning | Default encryption | Public access |
|------|--------|-----------|--------------------|---------------|
| `dr-secrets-us-east-2-049629455384` | us-east-2 | **Enabled** | SSE-KMS `alias/dr-secrets-us-east-2` | Block all |
| `dr-secrets-us-west-2-049629455384` | us-west-2 | **Enabled** | SSE-KMS `alias/dr-secrets-us-west-2` | Block all |

Only the DR role(s) (section 3) and the CRR role (section 4) may access them.

## 2. KMS customer-managed keys (create both)
| Alias | Region |
|-------|--------|
| `alias/dr-secrets-us-east-2` | us-east-2 |
| `alias/dr-secrets-us-west-2` | us-west-2 |

Key policy must allow use by the two DR roles and the CRR role in section 3–4
(`Encrypt`, `Decrypt`, `GenerateDataKey`, `DescribeKey`).

## 3. IAM roles for Databricks compute (create BOTH — serverless + classic)

### 3a. Role `dr-secrets-uc-role` (serverless / Unity Catalog credential)
Trust policy (Databricks UC master role + self-assume; **external id supplied by us**
after we create the UC storage credential):
```json
{ "Version": "2012-10-17", "Statement": [
  { "Effect": "Allow",
    "Principal": { "AWS": "arn:aws:iam::414351767826:role/unity-catalog-prod-UCMasterRole-14S5ZJVKOTYTL" },
    "Action": "sts:AssumeRole",
    "Condition": { "StringEquals": { "sts:ExternalId": "<WE-PROVIDE>" } } },
  { "Effect": "Allow",
    "Principal": { "AWS": "arn:aws:iam::049629455384:role/dr-secrets-uc-role" },
    "Action": "sts:AssumeRole" } ] }
```

### 3b. Role `dr-secrets-ec2-role` + instance profile `dr-secrets-instance-profile` (classic clusters)
Trust policy:
```json
{ "Version": "2012-10-17", "Statement": [
  { "Effect": "Allow", "Principal": { "Service": "ec2.amazonaws.com" }, "Action": "sts:AssumeRole" } ] }
```

### Attach this same policy `dr-secrets-access-policy` to BOTH roles above
```json
{ "Version": "2012-10-17", "Statement": [
  { "Sid": "S3", "Effect": "Allow",
    "Action": ["s3:GetObject","s3:PutObject","s3:DeleteObject","s3:GetObjectVersion",
               "s3:ListBucket","s3:GetBucketLocation"],
    "Resource": [
      "arn:aws:s3:::dr-secrets-us-east-2-049629455384",
      "arn:aws:s3:::dr-secrets-us-east-2-049629455384/*",
      "arn:aws:s3:::dr-secrets-us-west-2-049629455384",
      "arn:aws:s3:::dr-secrets-us-west-2-049629455384/*" ] },
  { "Sid": "KMS", "Effect": "Allow",
    "Action": ["kms:Encrypt","kms:Decrypt","kms:GenerateDataKey","kms:DescribeKey"],
    "Resource": [
      "arn:aws:kms:us-east-2:049629455384:key/*",
      "arn:aws:kms:us-west-2:049629455384:key/*" ],
    "Condition": { "StringEquals": { "kms:ResourceAliases": [
      "alias/dr-secrets-us-east-2","alias/dr-secrets-us-west-2" ] } } } ] }
```

## 4. CRR role + bidirectional replication

### Role `dr-secrets-crr-role`, trust `s3.amazonaws.com`
```json
{ "Version": "2012-10-17", "Statement": [
  { "Effect": "Allow", "Principal": { "Service": "s3.amazonaws.com" }, "Action": "sts:AssumeRole" } ] }
```
### Policy `dr-secrets-crr-policy` (attach to `dr-secrets-crr-role`)
```json
{ "Version": "2012-10-17", "Statement": [
  { "Sid": "SourceRead", "Effect": "Allow",
    "Action": ["s3:GetReplicationConfiguration","s3:ListBucket",
               "s3:GetObjectVersionForReplication","s3:GetObjectVersionAcl","s3:GetObjectVersionTagging"],
    "Resource": [
      "arn:aws:s3:::dr-secrets-us-east-2-049629455384","arn:aws:s3:::dr-secrets-us-east-2-049629455384/*",
      "arn:aws:s3:::dr-secrets-us-west-2-049629455384","arn:aws:s3:::dr-secrets-us-west-2-049629455384/*" ] },
  { "Sid": "DestWrite", "Effect": "Allow",
    "Action": ["s3:ReplicateObject","s3:ReplicateDelete","s3:ReplicateTags"],
    "Resource": [
      "arn:aws:s3:::dr-secrets-us-east-2-049629455384/*","arn:aws:s3:::dr-secrets-us-west-2-049629455384/*" ] },
  { "Sid": "KMS", "Effect": "Allow",
    "Action": ["kms:Decrypt","kms:Encrypt","kms:GenerateDataKey"],
    "Resource": ["arn:aws:kms:us-east-2:049629455384:key/*","arn:aws:kms:us-west-2:049629455384:key/*"],
    "Condition": { "StringEquals": { "kms:ResourceAliases": [
      "alias/dr-secrets-us-east-2","alias/dr-secrets-us-west-2" ] } } } ] }
```

### Replication rules (prefix `secrets/` only, delete-marker replication ON)
| Rule name | Source bucket | Dest bucket | Dest KMS |
|-----------|---------------|-------------|----------|
| `dr-secrets-crr-east-to-west` | `dr-secrets-us-east-2-049629455384` | `dr-secrets-us-west-2-049629455384` | `alias/dr-secrets-us-west-2` |
| `dr-secrets-crr-west-to-east` | `dr-secrets-us-west-2-049629455384` | `dr-secrets-us-east-2-049629455384` | `alias/dr-secrets-us-east-2` |

Both rules: filter prefix = `secrets/`, enable "replicate objects encrypted with KMS",
destination encryption = the destination-region key above.

---

## Handoff / who does what
- **AWS/cloud team (this ticket):** everything in sections 1–4.
- **We (Databricks side, after 1–4 exist):**
  - Create UC storage credential `dr_secrets_cred` (backed by `dr-secrets-uc-role`) in
    each workspace, then send you the generated **external id** for the trust policy (3a).
  - Create UC external locations `dr_secrets_east` → east bucket and `dr_secrets_west`
    → west bucket in each workspace.
  - Grant `ad-dr-spn` READ on the in-scope secret scopes (Databricks grant, not IAM).
  - Set `storage.primary_bucket` / `secondary_bucket` / `kms_key.*` in
    `config/secrets_dr_config.yaml`.
